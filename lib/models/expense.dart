class ExpenseModel {
  final String title;
  final double amount;
  final int id;
  final DateTime date; // Special Data type to store date

  ExpenseModel({
    required this.title,
    required this.amount,
    required this.date,
    
  })
}
